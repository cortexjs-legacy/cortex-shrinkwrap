var _globalNS = {
    
};

define = function(name, deps, fn) {

    
    var module = {
        exports: {

        }
    };

    var require  = function(name) {
        
    };

    fn(require, module, exports);

};
